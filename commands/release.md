---
description: Bump version, commit, tag, push, and refresh the plugin cache.
allowed-tools: Bash
---

Run the release script:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/release.sh
```

Show the output to the user.
